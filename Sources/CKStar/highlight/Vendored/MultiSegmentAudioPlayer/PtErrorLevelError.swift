
import UIKit
import TTLBGenerals
import NKWorking




protocol atoll_musicvideos_mamaral : NSObject {
    func modalPresentationStyle()
    func ptfContainer(_ tag : Int, _ upload : Bool)
    func symSharpInputCircle(_ id : Int, _ image : Int, _ tag : Int)
}




class PtErrorLevelError: UIView {
        
        
        
        
    @IBOutlet weak var lStreamIndex: UIImageView!
        
    @IBOutlet weak var uRemoveListeners: UIButton!
        
    @IBOutlet weak var yUpdateRunnable: UILabel!
    @IBOutlet weak var pNormalizedName: UIImageView!
        
        
        
    weak var resultBundleDelegate : atoll_musicvideos_mamaral!
        
    var kHasDouble = false
    var oWeexString = true
    var tFragmentRect = 0
    var pUnpackArg = ""
        
        
        
    deinit {
        resultBundleDelegate = nil
        capturedStatusBarProperties()
    }
        
        
    override func awakeFromNib() {
        super.awakeFromNib()
                
        self.frame = CGRectMake(0, 0, (aFullIntersection - 56)/3.0, 136)
                
        lStreamIndex.isUserInteractionEnabled = true
        let aOutputDirectory = UILongPressGestureRecognizer.init(target: self, action: #selector(asVoidPtr))
        aOutputDirectory.minimumPressDuration = 0.5
        aOutputDirectory.numberOfTouchesRequired = 1
        lStreamIndex.addGestureRecognizer(aOutputDirectory)
        lStreamIndex.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(updateMuteIndicatorHiddenState)))
                
        objectToCopy(#selector(symSharpBlender), "aah_linaliishere_effertz")
        objectToCopy(#selector(fontWeight), "addtion_perderti")
    }
    @objc func symSharpBlender(_ noty : Notification) {
        let bGetQualifier = noty.object as! Int
        if bGetQualifier == tag {
            oWeexString = true
            yUpdateRunnable.text = ""
            yUpdateRunnable.isHidden = true
        }
    }
    @objc func fontWeight(_ noty : Notification) {
        let bGetQualifier = noty.object as! Int
        if bGetQualifier == tag {
            oWeexString = false
            yUpdateRunnable.text = ""
            yUpdateRunnable.isHidden = true
            pNormalizedName.isHidden = false
        }
    }
    @objc func asVoidPtr(_ tap : UILongPressGestureRecognizer) {
        resultBundleDelegate.ptfContainer(tag, oWeexString)
    }
        
    func isAppSystem(_ model : PtToolStripItemPlacement?, _ image : UIImage?) {
        pUnpackArg = ""
        kHasDouble = true
        if model == nil {
            if image == nil {
                uRemoveListeners.isHidden = false
                lStreamIndex.isHidden = true
                kHasDouble = false
            }else {
                uRemoveListeners.isHidden = true
                lStreamIndex.isHidden = false
                lStreamIndex.image = image
            }
        }else {
            tFragmentRect = model!.gInsertUpdate
            uRemoveListeners.isHidden = true
            lStreamIndex.isHidden = false
            if model!.iGetFacets != nil && model!.iGetFacets!.count > 0 {
                lStreamIndex.getStatusBarHeight(model!.iGetFacets!)
                pUnpackArg = model!.iGetFacets!
            }else if model!.vAllSeries != nil {
                lStreamIndex.image = model!.vAllSeries
            }
        }
    }
    func fdFIymY(_ image : UIImage) {
        oWeexString = true
        kHasDouble = true
        lStreamIndex.image = image
        uRemoveListeners.isHidden = true
        lStreamIndex.isHidden = false
        hasMutualGroupThread()
        yUpdateRunnable.isHidden = false
        yUpdateRunnable.text = "0%"
        if tag == 1 {
            iSuppressAnimations.setAffectorType(image) { [weak self] gress in
                self?.yUpdateRunnable.text = String(format: "%.2f%%", gress * 95)
            } _: { [weak self] dics in
                self?.scriptingUsbDevices(dics)
            } _: { [weak self] in
                self?.updateMessageSendLogColumnTypes()
            }
        }else if tag == 2 {
            iSuppressAnimations.addEmulatorConfig(image) { [weak self] gress in
                self?.yUpdateRunnable.text = String(format: "%.2f%%", gress * 95)
            } _: { [weak self] dics in
                self?.scriptingUsbDevices(dics)
            } _: { [weak self] in
                self?.updateMessageSendLogColumnTypes()
            }
        }else if tag == 3 {
            iSuppressAnimations.getLengthAtIndex(image) { [weak self] gress in
                self?.yUpdateRunnable.text = String(format: "%.2f%%", gress * 95)
            } _: { [weak self] dics in
                self?.scriptingUsbDevices(dics)
            } _: { [weak self] in
                self?.updateMessageSendLogColumnTypes()
            }
        }
        pNormalizedName.isHidden = true
    }
    func scriptingUsbDevices(_ dics : NSDictionary) {
        self.resultBundleDelegate.symSharpInputCircle(self.tFragmentRect, dics["chylomicron"] as! Int, tag)
    }
    func updateMessageSendLogColumnTypes() {
        HeLoadPaletted.upLoadModel()
        oWeexString = false
                
        yUpdateRunnable.text = ""
        yUpdateRunnable.isHidden = true
        pNormalizedName.isHidden = false
    }
        
        
    func needsToRestoreRegistersIfException() {
        kHasDouble = false
        lStreamIndex.sd_cancelCurrentImageLoad()
        lStreamIndex.image = nil
        uRemoveListeners.isHidden = true
        lStreamIndex.isHidden = true
        yUpdateRunnable.isHidden = true
        hasMutualGroupThread()
    }
    func hasMutualGroupThread() {
        if tag == 1 {
            iSuppressAnimations.numberOfTrailingZeros()
        }else if tag == 2 {
            iSuppressAnimations.setCloneNodeCallback()
        }else if tag == 3 {
            iSuppressAnimations.symOutlinedElevation()
        }
    }
    @IBAction func rxDidChange(_ sender: Any) {
        resultBundleDelegate?.modalPresentationStyle()
    }
        
    @objc func updateMuteIndicatorHiddenState() {
        if kHasDouble {
            if pUnpackArg.count != 0 {
                HeLoadPaletted.btCollisionConfiguration([pUnpackArg], 0, false)
            }else {
                HeLoadPaletted.btCollisionConfiguration([lStreamIndex.image], 0, false)
            }
        }
    }
        
}
