
import UIKit
import TTLBGenerals




class PtKjQfrjhcBMxJt: UIView {
        
    @IBOutlet weak var nRuntimeEx: UIButton!
    var yPingFrequency = true
    @IBOutlet weak var cUniqueData: UIView!
        
    var wCameraOrientation:wCameraOrientation?
        
        
    deinit {
        print("`````````````````````PtKjQfrjhcBMxJt deinit")
    }
        
        
    override func awakeFromNib() {
        super.awakeFromNib()
        width = aFullIntersection
        height = nMagnetVoid
                
        cUniqueData.clickingOnWidgetListItemShowsCorrectWidgetUi(.tap) { [weak self] eActorLocation in
            self?.wCameraOrientation!(0)
            self?.removeFromSuperview()
        }
    }
    internal func forwardedSelectorsByClass(_ bool : Bool){
        if bool == false{
            nRuntimeEx.setTitle(roundPriceChange("streamify_appsflyer"), for: .normal)
        }
    }
        
    @IBAction func deleteBtnAction(_ sender: UIButton) {
        wCameraOrientation!(0)
        removeFromSuperview()
    }
        
    @IBAction func metaDataChanged(_ sender: UIButton) {
        if sender.tag == 3 {
            if yPingFrequency {
                yPingFrequency = false
                wCameraOrientation!(sender.tag)
            }
        }else{
            wCameraOrientation!(sender.tag)
        }
    }
        
        
}
